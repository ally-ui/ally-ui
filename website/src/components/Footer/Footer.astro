---
import AvatarList from './AvatarList.astro';

interface Props {
	currentFile: string;
}

const {currentFile} = Astro.props as Props;
---

<footer class="p-8 mt-auto border-t border-line">
	<AvatarList currentFile={currentFile} />
</footer>
